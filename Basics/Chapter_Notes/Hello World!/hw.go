package main //Every project has packages and main is the one that brings everything

import ( //This is where we import outside packages and libraries
	"fmt" // This is the input and output library
)

func main() { //Where all the code runs
	fmt.Println("Hello World!") //Print to terminal
}
